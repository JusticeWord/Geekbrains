#include <iostream>
#include <fstream>
#include <string>

#define DIAPASON(x, y) ((x >= 0 && x < y) || (x <= 0 && x > y)) ? cout << "Yes\n" : cout << "No\n"
#define ACCESS(x) cout << *x
#define LENGTH(x, y) cout << "Length of this array: " << sizeof(x) / sizeof(y)

using namespace std;

int main(int argc, char *argv[])
{
    int* buffer = (int *)malloc(sizeof(int) * 5);  
    int array[5] = {1, 2, 3, 4, 5};
    
    DIAPASON(-3, -5);
    LENGTH(array, int);
}
